# To run the app
pip install "fastapi[standard]"
# for dev env with autoload and debug
fastapi dev main.py
# (for prod setup more optimised and fast)
fastapi run main.py
# to work with async await need to install
# pip install aiosqlite
# pip install greenlet

# Part 10 Authentication
#orgon2 is best practice and modern
pip install "pwdlib[argon2]"
pip install pyjwt
pip install pydantic-settings

# Part 12 Image upload
pip install pillow

# Part 14 Password reset
# for async in place of smtplib to work with async to send email
pip install aiosmtplib

# to test dummy email sender
logon to https://mailtrap.io/
> create account
> and get email username and pass
> then put those in configuration to test

# Part 15 Making database production ready, use postgresql instead of sqlite
# and use alembic for database migration

# psycopg modern driver to connect with postgresql
pip install "psycopg[binary]"

# creating and install postgres using aws RDS here for now
# Steps to create rds instance postgres engine

# START
Go to VPC → Subnets
Filter by VPC = the SAME VPC you’ll use
✅ Public subnet checklist:
Route table has:
0.0.0.0/0 → igw-xxxx

Pick 2 subnets in different AZs
Example:
subnet-111 (ap-south-1a)
subnet-222 (ap-south-1b)

RDS → Subnet groups → Create
Name fastapi-public-subnet-group
VPC✅ SAME VPC as subnets
Subnets ✅ Only PUBLIC subnets
AZs✅ At least 2

RDS → Create Database
Public access ✅ YES
Subnet group ✅ fastapi-public-subnet-group
Security group ✅ allow 5432

Step 5: Security Group inbound
Postgres SQL 5432 My IP
Then test connection with test db DONE!

# END

# using alembic for database migration
pip install alembic

# -t async : to use async template and alembic_dir: directory of migrations
alembic init -t async alembic_dir

# set the Config at alembic files to configure alembic
# then run to generate alembic config
alembic revision --autogenerate -m "Creating initial schema"
# should generate the schema and version file inside alembic dir

# NOT RUN for deleting postgres public schema and create from scratch
psql

# to apply generated alembic config and create db
alembic upgrade head

# to check where alembic head is like gitlog
alembic current

# after adding likes to post schema
alembic revision --autogenerate -m "Added likes field to post schema"
alembic upgrade head
alembic current
alembic history

# in case of any issue, can roll back to previous version
alembic downgrade -1

# then after fixing upgrade head again

# Part 16 Uploading files i.e. Profile pics to AWS S3 bucket using boto3
#TODO in branch db_postgres we have code for part 15
# currently we are using local database from part 14 for testing later we merge that code.

pip install boto3
pip freeze > requirements.txt

S3 console > create bucket > uncheck block all public access (ACL top two can be checked)
> permissions > paste bucket policy GetObject to profile_pics/* dir

IAM console > Policies > create policy > paste policy put and delete object from that
bucket profile pics dir (least privilege) give any  name FastAPIBlogS3Policy and save

IAM User > create user > set permission >
'Attach policies directly' FastAPIBlogS3Policy policy > create
(If using on aws ecosystem IAM role also will work and app get the cred from roles)

In the created user > security credentials > create access keys and save
we will use that later in config, after that

# Now we change the code

#############################
# Important: When creating db on aws rds, and db_test is working, you see "internal server error"
then try to do the migration using alembic steps,
then populate db
Now db_postgres and files_to_s3 are merged
DONE!!

#TODO Testing WIP is there in testing_fastapi branch
# Testing Part 17
#
# Part 18
Deploying application to Virtual server like EC2, linode etc.
Step 1 Securing the server: applies to any server

sudo usermod -aG sudo ubuntu
groups ubuntu

Local System
ssh-keygen -t ed25519 -C "Punit - HP - 24-05-2026"
give file to save and pass
/c/Users/punit/.ssh/fastapi_hp

sudo systemctl restart ssh
ssh-copy-id -i ~/.ssh/fastapi_hp ubuntu@<ip>

cat ~/.ssh/fastapi_hp | ssh -i Key.pem ubuntu@<ip> "mkdir -p ~/.ssh && chmod 700 ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys"

# to add key to ssh agent
ssh-add ~/.ssh/fastapi_hp
# below command to run the agent
eval "$(ssh-agent -s)"

ssh -i ~/.ssh/fastapi_hp.pub ubuntu@<ip>
chmod 700 ~/.ssh/id_ed25591_fastapi_hp

chmod 700 ~/.ssh
chmod 600 ~/.ssh/*
ssh ubuntu@3.93.81.57
sudo nano /etc/ssh/sshd_config

timedatectl status
sudo hostnamectl set-hostname fastapi-server
sudo nano /etc/hosts
3.93.81.57 fastapi-server
sudo apt install ufw -y
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow ssh

sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

sudo ufw status verbose

# fail to ban prevents too many login attempts
sudo apt install fail2ban -y

# Step 2 specific to our web app
#TODO Will come later to deploy on EC2 Instances or any VPS

# Part 19 Deploy application using docker
# after logging in to ec2 instance
# in this part neon database us used for postgres that scales to zero if no request

# install docker
sudo apt update && sudo apt upgrade -y
sudo apt upgrade -y
sudo apt install -y docker.io git
docker --version

sudo systemctl enable docker
sudo systemctl start docker
sudo usermod -aG docker ubuntu
sudo usermod -aG docker $USER
# to apply changes without restart
newgrp docker
sudo systemctl status docker

git clone -b deploy_docker https://github.com/punitsharma01/FastAPI_Blog.git fastapi_blog
cd fastapi_blog

docker build -t fastapi-app .
docker run -d -p 8080:8080 --env-file .env fastapi-app

# to remove container and images
docker stop $(docker ps -a -q)
docker rm $(docker ps -a -q)
docker rmi $(docker images -a -q)

# to run at Google cloud
install gcloud-cli
# Docs https://docs.cloud.google.com/sdk/docs/install-sdk#deb
sudo apt-get update && sudo apt-get upgrade -y
sudo apt-get install ca-certificates gnupg curl

curl https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo gpg --dearmor -o /usr/share/keyrings/cloud.google.gpg

echo "deb [signed-by=/usr/share/keyrings/cloud.google.gpg] https://packages.cloud.google.com/apt cloud-sdk main" | sudo tee -a /etc/apt/sources.list.d/google-cloud-sdk.list

sudo apt-get update -y
sudo apt-get install google-cloud-cli

gcloud --version
gcloud auth login
gcloud config set project <PROJECT_ID>
gcloud config set project fastapi-blog
# OP: Updated property [core/project].

# List accounts whose credentials are stored on the local system:
gcloud auth list
# List the properties in your active gcloud CLI configuration:
gcloud config list

gcloud services enable run.googleapis.com
# if above command fails check google account and connect billing

gcloud services enable cloudbuild.googleapis.com
gcloud services enable artifactregistry.googleapis.com
gcloud services list --enabled

gcloud artifacts repositories create fastapi-repo \
--repository-format=docker \
--location=us-east4

# Created repository [fastapi-repo].

## Build and Push to Artifact Registry
gcloud builds submit \
    --tag us-east4-docker.pkg.dev/YOUR_PROJECT_ID/fastapi-repo/fastapi-app

## Deploy to Cloud Run
gcloud run deploy fastapi-service \
    --image us-east4-docker.pkg.dev/YOUR_PROJECT_ID/fastapi-repo/fastapi-app \
    --region us-east4 \
    --allow-unauthenticated

# It will fail, but service is created and env variables needed to provide from env
# to generate secrete for prod, never use same secrete in diff env
python3 -c "import secrets; print(secrets.token_hex(16))"

# on google cloud run console: copy
https://fastapi-service-6806.us-east4.run.app

# Edit and deploy new revision, provide env var values and deploy DONE!!!

# For change of update: code change > build > deploy
# or build and deploy can combine to one command
gcloud run deploy fastapi-service --source . --region=us-east4

# INFO
If docker file not present, gcloud uses build packs, it detects langauge and build container
automatically Limited to google